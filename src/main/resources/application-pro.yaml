spring:
    datasource:
        url: jdbc:h2:mem:starships
        username: root
        password: 1234
        driverClassName: org.h2.Driver
    #        url: ${MARIADB_URL}
    #        username: ${MARIADB_USER}
    #        password: ${MARIADB_PASSWORD}
    #        driver-class-name: org.mariadb.jdbc.Driver

    flyway:
        enabled: true

    jpa:
        hibernate:
            ddl-auto: none
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MariaDBDialect